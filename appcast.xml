<?xml version="1.0" standalone="yes"?>
<!--
  alt-tab-free [depaywall]: this in-tree appcast.xml has been TRUNCATED to an empty Sparkle feed
  skeleton. The fork publishes its real Sparkle feed OUT OF TREE (the gh-pages branch) via
  scripts/update_appcast.sh, so the live entries are NOT served from this tracked file (PLAN §3.3).

  OWNER ACTION (before publish): `git rm appcast.xml` and add `/appcast.xml` to .gitignore (the
  identity agent owns .gitignore). Serving the feed from the tree would (1) conflict on every
  upstream + fork release and (2) re-introduce lwouis-key-signed entries the fork's SUPublicEDKey
  cannot verify. It is left present-but-empty only so the tree stays buildable until removed.
-->
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
  <channel>
    <title>alt-tab-free</title>
    <language>en</language>
  </channel>
</rss>
